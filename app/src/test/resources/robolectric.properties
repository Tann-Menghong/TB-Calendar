# Robolectric ships images only up to API 35; the app targets 36, so tests pin a
# supported level rather than following targetSdk.
sdk=34

# The legacy SQLite shadow shares connection pointers across tests, which Room's
# invalidation tracker trips over after an in-memory database is closed.
sqliteMode=NATIVE
